{
  "name": "pyro",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:packages": "npm run build -w @pyro/contracts && npm run build -w @pyro/storage && npm run build -w @pyro/queue && npm run build -w @pyro/classifiers && npm run build -w @pyro/sdk",
    "build": "npm run build:packages && npm run build -w @pyro/gateway && npm run build -w @pyro/control-plane && npm run build -w @pyro/dashboard",
    "check": "npm run typecheck && npm test && npm run build",
    "dev": "concurrently -n gateway,control,dashboard -c blue,magenta,green \"npm run dev -w @pyro/gateway\" \"npm run dev -w @pyro/control-plane\" \"npm run dev -w @pyro/dashboard\"",
    "test": "npm run test -w @pyro/storage && npm run test -w @pyro/queue && npm run test -w @pyro/classifiers && npm run test -w @pyro/sdk && npm run test -w @pyro/gateway && npm run test -w @pyro/control-plane",
    "typecheck": "npm run build:packages && npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
